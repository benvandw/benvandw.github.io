title: "Your Epic Blog"
description: "A minimal bear-style blog"
url: "https://vandw.xyz"
baseurl: ""

# Author info (shown on homepage, optional)
author: "Ben"
tagline: "Cyber Security Engineering"

# Markdown / build settings
markdown: kramdown
permalink: /:title
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Jekyll plugins GitHub Pages supports natively (no Actions needed)
plugins:
  - jekyll-feed
  - jekyll-sitemap

feed:
  path: index.xml   # matches mrbruh.com's /index.xml RSS convention

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
